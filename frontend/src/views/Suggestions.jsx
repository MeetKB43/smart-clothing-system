import React from 'react';
import { ComingSoon, PrivateWrapper } from '../components/layouts';

const Suggestions = () => (
  <PrivateWrapper pageName="Suggestions">
    <ComingSoon pageName="Suggestions" />
  </PrivateWrapper>
);

export default Suggestions;
