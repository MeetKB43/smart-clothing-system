import React from 'react';
import { ComingSoon, PrivateWrapper } from '../components/layouts';

const Inventory = () => (
  <PrivateWrapper pageName="Inventory">
    <ComingSoon pageName="Inventory" />
  </PrivateWrapper>
);

export default Inventory;
